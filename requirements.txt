# artboard 核心依赖
#   pip install -r requirements.txt
Pillow>=10.0
playwright>=1.40

# 说明:
# - v1.9 起导出主路径为 Kiln 原生引擎(单文件,零 Python 依赖),本清单仅供
#   兜底路径(export_fallback.py)与机检(check_overflow.py)使用 playwright
# - Pillow 用于 CMYK 打印交付、公众号封面合并等纯像素工序
