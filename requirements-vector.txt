# artboard 按需依赖 —— 用到哪块装哪块
#   pip install -r requirements-vector.txt   (矢量/逆向全量)
#   pip install pikepdf                      (只出分层 AI 可编辑 PDF)

# ---- 矢量交付(HTML → PDF 枢纽 / SVG / EPS / .ai) ----
# 被 to_vector.py / ai_export.py / webhtml2vectoredit.py / vectoredit2webhtml.py 使用
pikepdf>=8.0

# ---- 矢量相似度自检(SSIM 与差异热区) ----
# 被 webhtml2vectoredit.py 使用;未装时相似度自检不可用
scikit-image>=0.22
numpy>=1.26

# ---- 二维码(生成 / 解析) ----
# 被 qr.py 使用
qrcode>=7.4
zxing-cpp>=2.2

# ---- 本地抠图(rembg 首次运行会再下载 onnx 模型) ----
# 被 cutout.py 使用;CPU 版够用,有 DirectML 显卡可换 rembg[dml]
rembg[cpu]>=2.0
