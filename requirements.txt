# artboard 核心依赖 —— 导出主路径(WPI)与兜底路径都需要
#   pip install -r requirements.txt
Pillow>=10.0
playwright>=1.40

# 说明:
# - WPI 源码版自身依赖由 scripts/setup_wpi.py --source-install 自动安装(同上两项)
# - 只出静态 PNG/GIF/PDF 时,装这两项即可
