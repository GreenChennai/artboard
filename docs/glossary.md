# artboard 词汇表

| 术语 | 含义 |
|---|---|
| 瘦身影子 | scaffold 默认模式:src/fonts、src/vendor 为指向 Skill 资产库的 NTFS 目录联接,项目零拷贝 |
| 联接(junction) | NTFS 目录重解析点;相对引用经 OS 穿透到 Skill 库,http/file 双通 |
| embed-fonts | `--embed-fonts`:把字体/vendor 真拷贝进项目的自包含模式(体积大,单件交付用) |
| pack | `scripts/pack.py`:穿透联接收集依赖 → 自包含 zip,HTML 引用自动改写 |
| 五问 | 开工前追问协议:用途/尺寸/风格/配色/素材(intake.md) |
| 设计简报 | 五问汇总的"生图式提示词",先给用户看再动工(design-prompt.txt) |
| 三模式 | 文案直出(A)/ 图片复刻(B)/ 换风格改配色(C)(replicate.md 为 B 的协议) |
| 证据链 | 复刻纪律:每个颜色/度量追溯到测量;token 必须带采样证据,裸眼估的不进 CSS(replicate.md) |
| 分区普查 | inspect_ref.py census 按区采样:flat fills=真填充/all pixels=小块/ink core=文字墨色(R1b) |
| 复刻比对 | compare.py 生成原图 vs 复刻图并排图(--region 局部放大+ΔRGB 数字),逐分区核对(replicate.md Step R4) |
| 风格分册 | 某视觉风格的规则集:色彩角色表+字体栈+字号阶+组件+禁则+案例(styles/) |
| 品类规范 | 某输出介质的规范:尺寸/字号下限/折线/盲区(formats/) |
| 原子混搭 | 分册章节按单关注点组织,可跨风格借用单个原子(如"瑞士版式+Y2K 配色") |
| 版权风险- | 爬虫来源素材的强制文件名前缀,交付时必须提醒更换(materials.md) |
| vision_mode | auto=Agent 视觉优先;local=强制本地 VQA/OCR(ADR 0003) |
| Kiln | 渲染导出引擎(v1.9 起):VellumBench 原生单文件,九格式 PNG/JPG/GIF/MP4/SVG/PDF/EPS/AI/PPTX;动画逐帧求值;中文真文本(CID) |
| settle | (WPI 时代概念,v1.9 起消亡)导出前的稳定化等待;Kiln 原生渲染无外部等待 |
| 孤字 | 段落末行仅 1 个汉字——排版硬禁(typography-rules.md) |
| tabular-nums | 等宽数字;数据列必须使用以垂直对齐 |
| 反 AI 味自检 | 12 条交付前清单(guardrails.md §5) |
| 矢量枢纽 | HTML 先打印成 Chromium 单页矢量 PDF,再衍生 SVG/EPS/转曲 PDF(ADR 0006) |
| 转曲(outline) | 文字变字形轮廓路径;矢量交付产物一律转曲,防换机换字体走样(ADR 0007) |
| -print.pdf | Chromium 原生打印 PDF:文字内嵌可编辑,AI 打开可改字(需装同款字体) |
| -outline.pdf | ghostscript 转曲 PDF:任何机器视觉一致,文字不可选 |
| AI 兼容 PDF | Illustrator 可直接打开编辑的 PDF;真 .ai = 在 AI 里打开它另存一次(ADR 0008) |
| SSIM 验收 | 转换产物栅格化后与基准截图算结构相似度,≥0.95 合格 ≥0.99 优秀(vector-export.md) |
| 矢量安全清单 | 16 种 CSS 原语转换实测表;**五条禁令**:硬切透明渐变 / mix-blend-mode / 渐变字 / conic-gradient / 渐变 alpha-stop 压圆角(vector-export.md §5) |
| 矢量直出 | v1.9 起 Kiln 原生直出 SVG/PDF/EPS/AI/PPTX(真文本可编辑),替代旧 WebHtml2VectorEdit 转换核心 |
| kiln import | v1.9 起的逆向:外部 PDF/AI → 场景图 → 规范化 HTML(kiln-cli import 子命令) |
| OCG | PDF 可选内容组;ai.pdf 的图层载体,Acrobat/浏览器可开关 |
| 蒙层 | 图片之后的同级渐变遮罩,单独成层保证盖在照片上 |
| 面板底色克隆 | 分层手术:有底色又含内容的元素,底色剥离进图形层,本体转结构 |
| 模式 P(海报循环) | 动效分册的循环模式:无缝循环 2–6s,首帧=末帧,终态=合格静态海报(animation.md §〇) |
| 模式 S(场景卡) | 动效分册的视频模式:插进口播之间的信息卡/图解卡,五段式一次性时间轴,全 finite,必须有出场 |
| 五段式 | 模式 S 时间轴契约:前置静置→入场→持住→出场→收尾静置(ADR-0012) |
| 前置静置 | 五段式第一段:空卡 ≥2.0s;v1.9 起纯作构图留白(Kiln 时间轴 t=0 即首帧,无录制偏移) |
| 录制偏移 | (WPI 时代概念,v1.9 起消亡)Kiln 逐帧求值 t=0 即首帧,无录制偏移 |
| 出场三原则 | 比入场短(0.4–0.6s)、方向延续(入向=出向)、stagger 反序(后进先出) |
| 语义断行 | 中文标题断点手动给:两行=两个 `.tl` span;断点选语法边界,禁止词中间劈开(typography-rules.md §一) |
| 逐行三合一 | 视频卡标题逐行 span:断行、换色、入场 stagger 三者共用同一 span 边界 |
| 生动性词汇表 | 科普/教程动效手法:图解渐进构建/逐词强调/数字计数/前后对比/聚光灯/Ken Burns/次要动作(animation.md §十) |
| 母阶 | 字号阶的单一回退基准:画布宽 1080,r=1.25/1.414 比例生成;分册缺项时才用(ADR-0014) |
| 栏宽换算 | 字号按**内容栏宽**而非画布宽换算:系数 = √(栏宽/1080);多栏版式(折页/杂志)必用(ADR-0014) |
| 动线 | 视线在画面上的移动路径:S 中轴 / Z / F / 放射 / 环;构图的第一决策(composition.md §一) |
| 骨架 | 主体在画布上的落位法:三分 / 黄金 1:1.618 / 中轴对称 / 对角 15–30° / 框中框 / 满版出血(composition.md §三) |
| 视觉重量 | 焦点评判的六个加权量:面积 40 / 对比 25 / 位置 15 / 孤立 10 / 方向 5 / 纹理 5(composition.md §二) |
| 基线单位 | 纵向节奏的最小单位 = round(正文字号 × 行高 ÷ 4) × 4;所有纵向间距取其 0.5/1/2/3 倍(composition.md §4.2) |
| 栏沟 | 相邻栏之间的间隙:画布 ≤800 用 16px、801–1440 用 24px、1441–2400 用 32px、>2400 用 48px(composition.md §4.1) |
| Rubric | 设计评审打分表:五维度 × 0–5 分 + 权重(A30/B25/C20/D15/E10),及格线 ≥3.5 且无单项 ≤1(ADR-0013) |
| 硬伤 | rubric 中直接计 0 的项:主标被盖不可读 / 字体回退 / 文字溢出 / 对比度 <2:1 / 元素毫无对齐(design-review-rubric.md §一) |
| 大字 | WCAG 2.2 定义:≥18pt(24px)常规 或 ≥14pt(18.66px)粗体 → 对比度阈值 3:1;其余算常规文字需 4.5:1(color-contrast.md §一) |
| 相对亮度 | Y = 0.2126R + 0.7152G + 0.0722B(通道先线性化);对比比 = (L1+.05)/(L2+.05)(color-contrast.md §二) |
| ΔL 速判 | 用 OKLCH 明度差估对比度:≥0.33 → 3:1、≥0.45 → 4.5:1、≥0.54 → 7:1;中性色精确,高饱和为近似(color-contrast.md §三) |
| 垫层 scrim | 文字压图时的可读性兜底层:全幅渐变 α ≤.75 / 局部色块 α .55–.70 / 二者叠加 + 文字收边投影 α ≤.50(color-contrast.md §四) |
| 色盲友好 | 约 8% 男性红绿色盲:颜色不得作唯一编码(加文字/形状);系列色相邻 OKLCH L 差 ≥0.12(color-contrast.md §五) |
| 大数字卡 | 单一巨数的呈现形态(不算图表):数字字距 0 或 −0.01em、单位字号 = 数字 30–40%、一屏 ≤3 个(dataviz.md §六) |
| LPI | 网线数:每英寸网点行数。铜版 150–175 / 胶版 133–150 / 报纸 85–100 / 喷绘无网点;图片 DPI = LPI×2(print-production.md §四) |
| 陷印 trapping | 补漏白:浅色向深色扩 0.25–0.5pt(胶印)以吸收套准误差;由印厂 RIP 完成,设计侧只能避免"必须精确套准"的构图(print-production.md §一) |
| 网点扩大 | 纸张吸墨导致网点变大的现象:铜版 15–20% / 胶版 25–30% / 新闻纸 30–40%;需提前提亮降饱和补偿(print-production.md §五) |
| 净空 clear space | logo 四周禁止进入的空白带 = logo 高(横版)或宽(竖版)的 25%;屏幕下限 12px、印刷下限 1.5mm(brand-system.md §1.1) |
| 品牌色阶 | 从一个品牌色生成的 10 阶:固定 H/C、按 OKLCH L 从 97% 铺到 21%,两端降 C(brand-system.md §二) |
| Reflow | 一稿多尺寸重排:装饰可等比缩放 / 文字重新定位 / 内容重新排布 / 图片重新裁切(responsive-reflow.md §一) |
| 母版+变体 | 多尺寸工作流:`<slug>-master.html` 存信息结构(文案唯一真相源),`-34`/`-11`/`-916` 存各自画布与字号(responsive-reflow.md §六) |
| 容器越框 | 文字画出其背景/边框容器的边界(但可能仍在画布内);与"出画布"是两类,处置不同(card-layout.md / ADR-0015) |
| painted 盒(绘制的盒) | 有非透明背景色或可见边框的元素。溢出的判据参照系:**只对 painted 盒**判"内容越界"——无绘制的纯文字元素内容略大于自身盒是行盒现象,不是缺陷 |
| 卡高公式 | 按最长条目反推容器高度:`padding×2 + Σ(行数×字号×行高) + 间距`,其中行数 = ceil(字数 ÷ 每行字数)(card-layout.md §三) |
| 收口 | 让容器在被撑破前截住内容的三种手段:`line-clamp`(行数)、`text-overflow:ellipsis`(单行)、`overflow:hidden`(兜底)。**不用于兜文字**——会裁掉半个字 |
| 安全区 | 视频里避开**字幕带 + 平台 UI 遮挡带**后剩下的可用区;9:16 = 左右 184 / 顶 230 / 底 576(video-safe-area.md) |
| 安全区三档 | 同一安全区的三级空间预算:**标准**(跨平台交集)/ **紧凑**(垂直放宽,左右不动)/ **极限**(四边 2.5%,几乎贴边)。降档必须声明代价(ADR-0017) |
| 降档 | 内容装不下时从标准档退到紧凑/极限档。顺序:精简/拆卡 → 紧凑 → 极限;**关键信息不下放**(主标/CTA 仍留在紧凑档内) |
| 机检门禁 | `check_overflow.py` 的几何量测(A 溢出自盒 / B 越出容器 / C 越出安全区);每次重导前必跑,3–5s、零 token,**不受"自检两个时机"限制**(ADR-0017) |
| 字幕带 | 成片叠加字幕所占的底部区域(9:16 ≈ 底部 30%、16:9 ≈ 16%、3:4 ≈ 18%);卡上内容不得进入 |
| UI 遮挡带 | 平台界面自身占用的区域:顶部状态栏/关注栏、底部文案与音乐栏、**右侧按钮列(9:16 宽 180px)** |
| 保守交集 | 多平台发布时取各平台安全区的最严值;9:16 = 左右 184 / 顶 230 / 底 576,用以一版通吃 |
| 豁免标记 | `data-allow-overflow` 属性:声明该子树是**装饰**(可越界);内容标记它等于承认"这块不是内容"(card-layout.md §六) |
| 生成层 | 从 brief 到"概念→视觉装置"的思考层,位于结构层(composition.md)之前;与"结构层/验收层"并列(design-thinking.md) |
| 主导动作 | 一张图唯一那个"最大胆、最记得住"的设计**决断**;与"焦点"不同——焦点是视觉落点(规律),主导动作是作者决断(意图)(design-thinking.md) |
| 视觉装置 | 把抽象概念落成可执行视觉的具体手段,如"用断裂的网格表达破碎"(design-thinking.md) |
| 四段翻译模型 | 生成层的流程契约:brief → 概念 → 视觉装置 → 版式落地,产出全部在既有分册落地且不豁免铁律 3(design-thinking.md) |
| 反套路清单 | 动笔前先写一句"这类需求最俗的做法(首俗)是什么"再主动避开;反 AI 味的正向手段(design-thinking.md §五) |
| 伪3D | 用 2D 手段(text-shadow 挤出/伪元素分层/clip-path 遮挡/透视变换)伪造厚度与纵深的**错觉**,非真 3D 建模;计入特效计数、一图一处纵深焦点(depth-3d.md) |
| 厚度带 | 长阴影/克隆层拼出的"实体侧壁",伪3D 的体积来源(depth-3d.md) |
| 统一光源 | 全图所有阴影/高光共享同一方向与软硬程度;tokens `--light-*` 单点定义,高程阴影全部从它派生(depth-3d.md §一) |
| 高程 | 元素"离地高度"的视觉级别,四级:E0 平贴/E1 微浮/E2 悬浮/E3 高悬,决定阴影长度与软硬;一图 ≤3 级(E0 不计)(depth-3d.md §一) |
| 景深 | 近实远虚:前景/主体/背景/空气层的虚实分级;位图虚化工序走 `imageops blur-bg`,分册只定"哪层虚多少"(depth-3d.md §五) |
| 减法阶梯 | 「更高级/更简约」类愿望的五级可执行删减:删/减/挪/精/点,按 L1→L5 逐级走,不跳级(revision-protocol.md) |
| 诊断式改稿 | 先定位模糊愿望对应哪一设计维度,再定点改,不重生成;与 pipeline.md 改稿协议(明确指令速查)互补(revision-protocol.md) |
| 高级感清单 | 正向「像专业设计」的 10 条信号清单;与反 AI 味 12 条(防守)分立——进攻 vs 防守,两清单不合并(revision-protocol.md §8) |
| 双语层级 | 中英并置的主从关系,按"关系四分"先定关系再定参数:主从/眉题/对等/装饰层;【硬】一张图只用一种关系(bilingual-typography.md §一) |
| 视觉密度匹配 | 中英并排时补偿"中文看起来更黑"的**可算**调整:先字重、再字号(×1.03 级)、后字距;50% 缩放目测验收(bilingual-typography.md §三) |
| 语气(排版) | 排版气质四型:Power/Quiet/Playful/Editorial;intake 人格三词映射到语气,再落到字号/字重/字距/对齐/行高参数(typographic-expression.md §一) |
| 尺度天窗 | 主标 vs 正文可拉到 3–6 倍的戏剧性落差;区别于硬规则的"地板"(主标 ≥1.5×)——地板管"分得清",天窗管"喊得响"(typographic-expression.md §二) |
| 图像角色 | 图片进版面前先定定位:主体/背景/纹理/氛围/对比对五类;角色决定色调、裁切、景深、光影的全部处理选择(image-language.md) |
| 处理链 | 位图工序的固定顺序:矫正→几何→抠图→色调→合成→压缩,不许倒序(image-language.md) |
| 几何构成法 | 用基本形+布尔+变换构造矢量图形的方法,构成四法:等分(点环/放射)/栅格/黄金比/负形(负空间拼法);配构成纪律(放射线 ≤24 根等)(vector-drawing.md §4.2) |
| 图标语言 | 图标组的统一规范(Tabler 基准 7 项:线宽/圆头语言/圆角/视觉尺寸/像素对齐等);**混源禁令**:同屏不混第二来源图标库(vector-drawing.md §4.3) |
| 设计预算 | 一张图允许投入的复杂度上限(防过度设计,生成期可数);预算满了还想加必须"先还后借"——新加一种手段必须删一种旧的(design-thinking.md §九) |
| 多稿同出 | **备选模式(Opt-in,默认单稿)**:仅由指令或用户明确要求触发,一次 4 变体+一张 2×2 联络表,选定即转正;4 稿总耗时 ≤单稿×2(实测 1.27×)(multi-draft.md) |
| 变体 | 多稿中的一个方向版本(默认 4 个);彼此沿受控轴各差一个维度,内容共享不重写(multi-draft.md) |
| 受控轴 | 变体之间允许变化的单一维度:构图/配色/主视觉/大胆程度,每稿只动一轴——差异可解释、成本可控(multi-draft.md) |
| 联络表 | 把 N 个变体拼成一张(默认 2×2)带标签的对比图,供一眼选择;`imageops montage` 子命令(multi-draft.md) |
| 转正 | 用户选定某变体后,将其提升为项目基线 index.html,其余归档;改稿仍走定点改,不为选方向回炉 4 稿(multi-draft.md) |
| 混搭选择 | 用户选择"X 的构图 + Y 的配色"时,以其中一版为基线套另一版的差异区(multi-draft.md) |
| 体积预算 | 新能力(如伪3D)允许占用的额外体积上限:目标 ≤10MB/硬上限 ≤200MB;伪3D 执行轮实测增量 ≈23KB(ADR-0020) |
| Kiln 双车道 | Kiln 导出双车道:`--engine auto`(默认)浏览器在位走系统 Edge/Chrome 高保真道,缺席回退 `--engine native` 自研道;伪3D 原语多数只在浏览器道成立,自研道失效是**静默**的(depth-3d.md §六) |
| 症状词 | 「不够高级/太平/差点意思」这类说不出要什么的模糊表述;改稿先译成维度诊断再动手,不硬猜(revision-protocol.md §2) |
| 三方向 | 说不清时的生成兜底:同一 brief 出保守正确/激进表达/折中巧思三个可区分方向;生成期归本条,改稿期呈现归 revision-protocol §5,批量出稿归 multi-draft(design-thinking.md §六) |
| 软提示四行 | 写进 design-prompt.txt 的固定四行:【人格】三词/【参照】一个视觉参照/【主导动作】一句话/【反参照】不要什么;人格三词禁用评价词(高级/大气/好看不可执行)(design-thinking.md §七) |
| 图形化展示字 | 「字体即图形」:文字已当图形拼装的展示字,行高 0.85–1.1 **窄豁免**(仅此一处,非放宽),不承担识读;阅读型标题仍守 1.1–1.3(typographic-expression.md §五) |
| 画布内裁切窗 | 真·出血裁字默认禁的替代:窗口留在画布内 `overflow:hidden` + `data-allow-overflow`,实现同一观感且不越安全边距(typographic-expression.md §五) |
| 24px 并排校验法 | 图标组画完必做:把整组并排缩到 24px 看粗细/密度/风格,看不出任何一枚异类才算过(vector-drawing.md §4.3) |
| 一体字 | 内置西文、中西文同库同高的字体(如嗷呜等高体):西文与汉字同高,**只占一个字体名额**,是免配平的捷径;中西文合计仍 ≤3 款(bilingual-typography.md §二) |
| 50% 缩放验收法 | 密度是视觉量、机检替代不了:中英混排整图缩到 50% 目测灰度是否均衡(bilingual-typography.md §三) |
| 引号分派 | 引号按风格分派(总纲 D6):通用/印刷默认弯引号,国潮/杂志/UI 风可用直角引号「」;同项目统一;英文永远弯引号;U+0022 直引号永远禁(bilingual-typography.md §六) |
| 缩略图可读性 | 信息流场景必过:把图缩到 15% 宽,主信息(主标+关键数字)仍可辨才算过;不过就加大主标或减信息,不给主标加描边(composition.md §二) |
| 系列 token 契约 | 同主题、非同品牌的 ≥3 张系列开工前先确认的契约:固定 tokens/字体/网格/构图母版——"系列感"的落地机制,复用品牌七项的变体,暂不建册(brand-system.md §五) |
| 视距换算公式 | 字面高 H 与视距 D 的两条经验公式,推出"最低可辨/舒适阅读"两档字号;有品类表(formats/)时优先用表(color-contrast.md §八) |
| 失效台账三态 | docs/failures.md 的修复状态三档:**已修**(注明版本)/ **已探明未修**(走 ITERATION 环)/ **绕行中**;只记真实发生过的失效(docs/failures.md) |

---

## 术语消歧

> 以下 4 组术语在全库被混用,是大量"数字打架"的语义根因。遇到时按本表取义。

| 混用组 | 成员 | 消歧 | 真相源 |
|---|---|---|---|
| **尺寸类** | **预设** / **物料** / **品类** | **预设** = 画布参数(宽/高/scale),18 个键;**物料** = 真实投放物(社媒/电商/印刷/办公/广告,47 种);**品类** = 输出介质族,有独立规范分册(名片/A4/三折页/易拉宝/PPT,5 类) | 预设:`scripts/scaffold.py:29-50` SIZES;物料:`material-catalog.md`;品类:`formats/` |
| **风格类** | **风格** / **风格方向** | **风格** = 已建分册,含可执行规则集(色板/字体栈/字号阶/组件/禁则),现 4 册;**风格方向** = `styles-catalog.md` 的 123 个气质词,只供选型,**不可执行** | `styles/` vs `styles-catalog.md` |
| **字号类** | **大字** / **主标** | **大字** = WCAG 的对比度豁免类别(≥24px 常规 / ≥18.66px 粗体),是**阈值概念**;**主标** = 字号阶第一级,是**层级概念**。1080 基准下只有主标和副标够"大字",正文/标签/页脚都不算 | `color-contrast.md` §一 |
| **缩放目测类** | **50% 缩放法** / **24px 并排校验** / **缩略图可读性** | 三种不同对象的缩放目测,别混用:**50% 缩放法** = 中英混排整图的灰度均衡(bilingual-typography §三);**24px 并排校验** = 图标组的同族一致性(vector-drawing §4.3);**缩略图可读性** = 信息流缩略状态(15% 宽)主信息仍可辨(composition §二)。对象分别是密度/图标/主信息 | bilingual-typography §三 / vector-drawing §4.3 / composition §二 |
| **换算类** | **等比换算** / **栏宽换算** | **等比**(按画布宽线性)已废,三折页/长图均失效;**栏宽换算**(系数 = √(栏宽/1080),多栏用栏宽)为唯一口径 | ADR-0014 + `responsive-reflow.md` §二 |
| **安全类** | **安全边距** / **安全区** | **安全边距** = 画布四周的通用内边距(6%/8%),防裁切与贴边,静态图也用;**安全区** = 视频专有,躲开字幕带与平台 UI 后的可用区(9:16 左右 184/顶 230/底 576)。前者是"离边多远",后者是"哪块能用" | `guardrails.md §1` vs `video-safe-area.md` |
| **溢出类** | **出画布** / **出容器** | **出画布** = 内容被 `.poster{overflow:hidden}` 裁掉,自检一直在查;**出容器** = 文字画出卡片背景/边框但仍未出画布,此前无任何判据(ADR-0015)。后者更常见也更隐蔽 | `card-layout.md` + `check_overflow.py` |
| **检查类** | **机检** / **自检** | **机检** = 脚本几何量测(A/B/C 类),零 token,每次重导前必跑;**自检** = Agent Read 看图与评审,只在首版完成 / 用户要求两个时机做。铁律 5 的"只在两个时机"**只约束自检** | `pipeline.md` Step 6.0 / 6.1(ADR-0017) |
| **安全区类** | **标准档** / **紧凑档** / **极限档** | 同一安全区的三级预算,不是三个不同的概念。9:16:标准 = 左右184 顶230 底576;**紧凑** = 左右仍184、顶134 底480(右侧按钮列压不动);**极限** = 四边 2.5%(用可见性换空间) | `video-safe-area.md §二` |

### 已作废表述

| 表述 | 位置 | 取代者 |
|---|---|---|
| 长图字号「×1.6」 | `guardrails.md:30` | 2400 → 系数 1.49(ADR-0014) |
| 长图字号「×~1.8」 | `data-longform.md:25` | 同上 |
| 「其他宽度等比换算」 | `guardrails.md:30` | 「按画布宽/栏宽开方换算」 |
| 矢量安全清单「三条禁令」 | `glossary.md` 旧版 | **五条**(`vector-export.md:141`) |
